{
  "name": "devarena-live-root",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "postinstall": "npm install --prefix server",
    "build": "npm run build --prefix client",
    "start": "node server/src/server.js",
    "dev": "npm run dev --prefix server"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "socket.io": "^4.7.5"
  }
}
